[package]
name = "toml-std"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.97"
config-store = { path = "../..", default-features = false, features = ["toml"] }
serde = { version = "1.0.219", features = ["derive"] }
